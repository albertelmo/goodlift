# PWA 아이콘 업데이트 가이드 (재설치 불필요)

## 📋 개요

PWA를 재설치하지 않고 앱 아이콘을 업데이트하는 방법을 설명합니다.

## 🎯 방법 1: 쿼리 파라미터 사용 (권장)

아이콘 URL에 버전 쿼리 파라미터를 추가하여 브라우저가 새로운 아이콘을 인식하도록 합니다.

### 구현 방법

1. **manifest.json 수정**
```json
{
  "icons": [
    {
      "src": "/img/logo.png?v=2",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any maskable"
    },
    {
      "src": "/img/logo.png?v=2",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any maskable"
    }
  ]
}
```

2. **Service Worker에서 manifest.json 캐시 제외**
   - manifest.json을 항상 네트워크에서 가져오도록 설정

3. **Service Worker 버전 업데이트**
   - SW 버전을 변경하여 캐시 무효화

## 🎯 방법 2: 파일명 변경

새로운 아이콘 파일을 다른 이름으로 저장하고 manifest.json에서 참조를 변경합니다.

### 구현 방법

1. 새 아이콘 파일 생성: `/img/logo-v2.png`
2. manifest.json에서 경로 변경
3. Service Worker 버전 업데이트

## 🎯 방법 3: manifest.json 버전 필드 추가

manifest.json에 version 필드를 추가하여 브라우저가 변경을 감지하도록 합니다.

### 구현 방법

```json
{
  "name": "스탠다드 멤버스",
  "version": "1.0.1",
  "icons": [...]
}
```

## ✅ 권장 구현 (방법 1 + 방법 3 조합)

가장 효과적인 방법은 쿼리 파라미터와 버전 필드를 함께 사용하는 것입니다.
