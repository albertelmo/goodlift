# 트레이너 장부 탭 설계 문서

## 📋 개요
장부 기능이 'on'인 트레이너에게 "나의 회원" 다음 탭에 장부 탭을 추가합니다. 트레이너별로 고정지출, 변동지출, 급여를 관리할 수 있는 독립적인 장부 시스템을 구축합니다.

## 🎯 목표
- 장부 기능이 활성화된 트레이너만 장부 탭 접근 가능
- 트레이너 본인의 고정지출, 변동지출, 급여만 관리
- SU 장부와 유사한 UI/UX 제공 (단, center 필드 제외)

## 📊 기능 범위

### 1. 탭 표시 조건
- 트레이너의 `ledger` 필드가 `'on'`인 경우에만 장부 탭 표시
- "나의 회원" 탭 다음에 배치

### 2. 데이터베이스 구조

#### 2.1 트레이너 고정지출 테이블 (`trainer_fixed_expenses`)
```sql
CREATE TABLE trainer_fixed_expenses (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  trainer VARCHAR(100) NOT NULL,          -- 트레이너 username
  month VARCHAR(7) NOT NULL,              -- 연월 (YYYY-MM)
  item VARCHAR(200) NOT NULL,             -- 항목명
  amount INTEGER NOT NULL DEFAULT 0,      -- 금액 (원)
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

**인덱스**: `idx_trainer_fixed_expenses_trainer_month` ON (trainer, month)

#### 2.2 트레이너 변동지출 테이블 (`trainer_variable_expenses`)
```sql
CREATE TABLE trainer_variable_expenses (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  trainer VARCHAR(100) NOT NULL,          -- 트레이너 username
  month VARCHAR(7) NOT NULL,              -- 연월 (YYYY-MM)
  date DATE,                              -- 날짜 (선택사항)
  item VARCHAR(200) NOT NULL,             -- 항목명
  amount INTEGER NOT NULL DEFAULT 0,      -- 금액 (원)
  note VARCHAR(500),                      -- 비고
  tax_type VARCHAR(50),                   -- 세금 타입 ('vat', 'corporate_tax' 또는 NULL)
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

**인덱스**: `idx_trainer_variable_expenses_trainer_month` ON (trainer, month)

#### 2.3 트레이너 급여 테이블 (`trainer_salaries`)
```sql
CREATE TABLE trainer_salaries (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  trainer VARCHAR(100) NOT NULL,          -- 트레이너 username
  month VARCHAR(7) NOT NULL,              -- 연월 (YYYY-MM)
  item VARCHAR(200) NOT NULL,             -- 항목명 (예: "기본급여", "성과급" 등)
  amount INTEGER NOT NULL DEFAULT 0,      -- 금액 (원)
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

**인덱스**: `idx_trainer_salaries_trainer_month` ON (trainer, month)

### 3. 표시할 데이터

#### 3.1 고정지출
- 트레이너 본인이 등록한 고정지출만 표시
- 월별 합계 표시

#### 3.2 변동지출
- 트레이너 본인이 등록한 변동지출만 표시
- 세금 타입별 분류 가능 (부가세, 법인세)
- 월별 합계 표시

#### 3.3 급여
- 트레이너 본인의 급여만 표시
- 항목별 급여 표시
- 월별 합계 표시

#### 3.4 지출 종류별 합계
- 고정지출 합계
- 변동지출 합계
- 급여 합계
- 전체 합계

### 4. 제외할 기능
- 센터별 매출 (트레이너 장부에는 매출 정보 없음)
- 월순익/연간순익 계산 (매출 정보 없으므로 계산 불가)
- 월별 정산 (SU 전용)
- 이전월 데이터 복사 (SU 전용)

## 🗂️ 파일 구조

### 새로 생성할 파일
- `public/js/trainer-ledger.js` - 트레이너 장부 모듈
- `backend/trainer-ledger-db.js` - 트레이너 장부 DB 모듈

### 수정할 파일
- `public/js/main.js` - 트레이너 탭에 장부 탭 추가
- `backend/server.js` - 트레이너 장부 API 엔드포인트 추가

## 🔧 구현 상세

### 1. 백엔드 - DB 모듈 (`backend/trainer-ledger-db.js`)

#### 1.1 테이블 생성 함수
- `createTrainerFixedExpenseTable()` - 고정지출 테이블 생성
- `createTrainerVariableExpenseTable()` - 변동지출 테이블 생성
- `createTrainerSalaryTable()` - 급여 테이블 생성
- `initializeDatabase()` - 모든 테이블 초기화

#### 1.2 CRUD 함수
**고정지출**:
- `getTrainerFixedExpenses(filters)` - 조회
- `addTrainerFixedExpense(expense)` - 추가
- `updateTrainerFixedExpense(id, updates)` - 수정
- `deleteTrainerFixedExpense(id)` - 삭제

**변동지출**:
- `getTrainerVariableExpenses(filters)` - 조회
- `addTrainerVariableExpense(expense)` - 추가
- `updateTrainerVariableExpense(id, updates)` - 수정
- `deleteTrainerVariableExpense(id)` - 삭제

**급여**:
- `getTrainerSalaries(filters)` - 조회
- `addTrainerSalary(salary)` - 추가
- `updateTrainerSalary(id, updates)` - 수정
- `deleteTrainerSalary(id)` - 삭제

### 2. 백엔드 - API 엔드포인트 (`backend/server.js`)

#### 2.1 고정지출 API
- `GET /api/trainer/fixed-expenses?month=YYYY-MM` - 조회 (현재 로그인한 트레이너만)
- `POST /api/trainer/fixed-expenses` - 추가
- `PATCH /api/trainer/fixed-expenses/:id` - 수정
- `DELETE /api/trainer/fixed-expenses/:id` - 삭제

#### 2.2 변동지출 API
- `GET /api/trainer/variable-expenses?month=YYYY-MM` - 조회 (현재 로그인한 트레이너만)
- `POST /api/trainer/variable-expenses` - 추가
- `PATCH /api/trainer/variable-expenses/:id` - 수정
- `DELETE /api/trainer/variable-expenses/:id` - 삭제

#### 2.3 급여 API
- `GET /api/trainer/salaries?month=YYYY-MM` - 조회 (현재 로그인한 트레이너만)
- `POST /api/trainer/salaries` - 추가
- `PATCH /api/trainer/salaries/:id` - 수정
- `DELETE /api/trainer/salaries/:id` - 삭제

#### 2.4 권한 체크
- 모든 API에서 현재 로그인한 트레이너의 username 확인
- 본인 데이터만 조회/수정/삭제 가능하도록 제한

### 3. 프론트엔드 - 탭 추가 (`public/js/main.js`)

#### 3.1 트레이너 탭 배열 수정
```javascript
const trainerTabs = [
    { label: '나의 수업', id: '📅', content: '<div id="session-calendar"></div>' },
    { label: '전체 수업', id: 'Today', content: '<div id="admin-day-calendar-root"></div>' },
    { label: '나의 회원', id: '👤', content: '<div id="my-member-list"></div>' },
    { label: '장부', id: '📖', content: '<div id="trainer-ledger-root"></div>' } // 추가
];
```

#### 3.2 탭 렌더링 로직 수정
```javascript
// 트레이너 정보 조회 시 ledger 필드 확인
if (trainer.ledger === 'on') {
    // 장부 탭 포함
    tabs = trainerTabs;
} else {
    // 장부 탭 제외
    tabs = trainerTabs.filter(tab => tab.id !== '📖');
}
```

#### 3.3 탭 클릭 핸들러 추가
```javascript
} else if (tabId === '📖') {
    trainerLedger.render(tabContent.querySelector('#trainer-ledger-root') || tabContent);
}
```

### 4. 프론트엔드 - 트레이너 장부 모듈 (`public/js/trainer-ledger.js`)

#### 4.1 모듈 구조
```javascript
export const trainerLedger = {
  render
};
```

#### 4.2 주요 함수
- `render(container)` - 장부 화면 렌더링
- `loadLedgerData()` - 트레이너 장부 데이터 로드
- `renderFixedExpenses()` - 고정지출 렌더링
- `renderVariableExpenses()` - 변동지출 렌더링
- `renderSalaries()` - 급여 렌더링
- `renderExpenseSummary()` - 지출 종류별 합계 표시
- `showFixedExpenseAddModal()` - 고정지출 추가 모달
- `showVariableExpenseAddModal()` - 변동지출 추가 모달
- `showSalaryAddModal()` - 급여 추가 모달

#### 4.3 UI 구성
```
┌─────────────────────────────────────────┐
│ 📖 장부        [◀ 2025년 1월 ▶]        │
├─────────────────────────────────────────┤
│ 지출 종류별 합계                          │
│ ┌─────────┬─────────┬─────────┐        │
│ │ 고정지출│ 변동지출│ 급여   │        │
│ │ 500,000 │ 300,000 │ 800,000│        │
│ └─────────┴─────────┴─────────┘        │
│ 전체 합계: 1,600,000원                   │
├─────────────────────────────────────────┤
│ 고정지출                    [추가]       │
│ ┌─────────────────────────────────┐    │
│ │ 항목      │ 금액                │    │
│ │ 임대료    │ 500,000            │    │
│ └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│ 변동지출                    [추가]       │
│ ┌─────────────────────────────────┐    │
│ │ 항목      │ 금액    │ 세금타입  │    │
│ │ 광고비    │ 200,000 │ 부가세    │    │
│ └─────────────────────────────────┘    │
├─────────────────────────────────────────┤
│ 급여                        [추가]       │
│ ┌─────────────────────────────────┐    │
│ │ 항목      │ 금액                │    │
│ │ 기본급여  │ 2,000,000          │    │
│ └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

## 🔍 데이터 필터링 로직

### 1. 고정지출 데이터
- `trainer_fixed_expenses` 테이블에서 `trainer` 필드로 필터링
- 현재 로그인한 트레이너의 username으로 조회

### 2. 변동지출 데이터
- `trainer_variable_expenses` 테이블에서 `trainer` 필드로 필터링
- 현재 로그인한 트레이너의 username으로 조회

### 3. 급여 데이터
- `trainer_salaries` 테이블에서 `trainer` 필드로 필터링
- 현재 로그인한 트레이너의 username으로 조회

## ⚠️ 주의사항

1. **권한 체크**: 트레이너는 본인 데이터만 조회/수정/삭제 가능
2. **데이터 보안**: 다른 트레이너의 데이터 접근 방지
3. **독립성**: SU 장부와 완전히 분리된 독립적인 시스템
4. **center 필드 제외**: 모든 테이블에서 center 필드 사용 안 함

## 📝 구현 순서

1. ✅ 설계 문서 작성
2. ⬜ `trainer-ledger-db.js` 모듈 생성 (테이블 생성 및 CRUD 함수)
3. ⬜ `backend/server.js`에 트레이너 장부 API 엔드포인트 추가
4. ⬜ `trainer-ledger.js` 모듈 생성 (UI 렌더링)
5. ⬜ `main.js`에 장부 탭 추가 및 조건부 렌더링
6. ⬜ 고정지출 추가/수정/삭제 기능
7. ⬜ 변동지출 추가/수정/삭제 기능
8. ⬜ 급여 추가/수정/삭제 기능
9. ⬜ 지출 종류별 합계 계산 및 표시
10. ⬜ 테스트 및 검증

## 🎨 UI/UX 고려사항

1. **SU 장부 탭과 유사한 디자인** 유지
2. **반응형 디자인** 적용
3. **로딩 상태** 표시
4. **에러 처리** 및 사용자 피드백
5. **날짜 네비게이션** (이전월/다음월)
6. **모달을 통한 추가/수정** (SU 장부와 동일한 패턴)

## 🔄 향후 확장 가능성

1. 이전월 데이터 복사 기능
2. 월별 비교 기능
3. 연간 통계 기능
4. 엑셀 내보내기 기능
